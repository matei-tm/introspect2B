name: 2. Deploy Terraform Infrastructure

on:
  workflow_dispatch:
    inputs:
      action:
        description: 'Terraform action to perform'
        required: true
        default: 'apply'
        type: choice
        options:
          - plan
          - apply
          - destroy
  workflow_call:
    inputs:
      action:
        description: 'Terraform action to perform'
        required: false
        default: 'apply'
        type: string


env:
  AWS_REGION: us-east-1
  TERRAFORM_VERSION: 1.6.0

jobs:
  terraform:
    name: Terraform ${{ github.event.inputs.action || 'plan' }}
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: iac/terraform
    
    permissions:
      id-token: write
      contents: read
      pull-requests: write
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Setup Terraform
        uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: ${{ env.TERRAFORM_VERSION }}

      - name: Terraform Format Check
        id: fmt
        run: terraform fmt -check -recursive
        continue-on-error: true

      - name: Terraform Init
        id: init
        run: terraform init

      - name: Terraform Validate
        id: validate
        run: terraform validate -no-color

      - name: Terraform Plan
        id: plan
        if: github.event.inputs.action != 'destroy'
        run: |
          set +e
          terraform plan -no-color -input=false -out=tfplan 2>&1 | tee plan.log
          PLAN_EXIT_CODE=${PIPESTATUS[0]}
          echo "exit_code=$PLAN_EXIT_CODE" >> $GITHUB_OUTPUT
          
          if [ $PLAN_EXIT_CODE -eq 0 ]; then
            echo "✅ Plan succeeded with no changes"
            exit 0
          elif [ $PLAN_EXIT_CODE -eq 2 ]; then
            echo "✅ Plan succeeded with changes"
            exit 0
          else
            echo "❌ Plan failed with exit code $PLAN_EXIT_CODE"
            echo ""
            echo "=== Last 100 lines of plan output ==="
            tail -100 plan.log
            exit 1
          fi
        continue-on-error: true

      - name: Terraform Plan (Destroy)
        id: plan-destroy
        if: github.event.inputs.action == 'destroy'
        run: terraform plan -destroy -no-color -input=false
        continue-on-error: true

      - name: Comment PR with Terraform Plan
        uses: actions/github-script@v7
        if: github.event_name == 'pull_request' && (steps.plan.outputs.exitcode == 0 || steps.plan-destroy.outputs.exitcode == 0)
        env:
          PLAN: "${{ steps.plan.outputs.stdout || steps.plan-destroy.outputs.stdout }}"
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const output = `#### Terraform Format and Style 🖌\`${{ steps.fmt.outcome }}\`
            #### Terraform Initialization ⚙️\`${{ steps.init.outcome }}\`
            #### Terraform Validation 🤖\`${{ steps.validate.outcome }}\`
            #### Terraform Plan 📖\`${{ steps.plan.outcome || steps.plan-destroy.outcome }}\`

            <details><summary>Show Plan</summary>

            \`\`\`terraform
            ${process.env.PLAN}
            \`\`\`

            </details>

            *Pushed by: @${{ github.actor }}, Action: \`${{ github.event_name }}\`*`;

            github.rest.issues.createComment({
              issue_number: context.issue.number,
              owner: context.repo.owner,
              repo: context.repo.repo,
              body: output
            })

      - name: Terraform Apply
        if: |
          (github.event.inputs.action == 'apply' || 
           (github.event_name == 'push' && github.ref == 'refs/heads/main')) &&
          steps.plan.outcome == 'success'
        run: |
          set -x
          terraform apply -auto-approve -input=false 2>&1 | tee apply.log
          exit ${PIPESTATUS[0]}

      - name: Terraform Destroy
        if: github.event.inputs.action == 'destroy' && steps.plan-destroy.outcome == 'success'
        run: terraform destroy -auto-approve -input=false

      - name: Update kubeconfig
        if: |
          (github.event.inputs.action == 'apply' || 
           (github.event_name == 'push' && github.ref == 'refs/heads/main'))
        run: |
          # Wait for cluster to be available
          for i in {1..40}; do
            if aws eks describe-cluster --name materclaims-cluster --region ${{ env.AWS_REGION }} &>/dev/null; then
              echo "Cluster is available"
              aws eks update-kubeconfig --region ${{ env.AWS_REGION }} --name materclaims-cluster
              exit 0
            fi
            echo "Waiting for cluster to be available... (attempt $i/40)"
            sleep 30
          done
          echo "Cluster not available after 20 minutes"
          exit 1

      - name: Verify cluster access
        if: |
          (github.event.inputs.action == 'apply' || 
           (github.event_name == 'push' && github.ref == 'refs/heads/main'))
        run: |
          echo "Verifying cluster access..."
          kubectl get nodes
          echo "Checking metrics-server..."
          kubectl get pods -n kube-system | grep metrics-server || echo "metrics-server not found (may still be deploying)"

      - name: Output cluster info
        if: |
          github.event.inputs.action == 'apply' || 
          (github.event_name == 'push' && github.ref == 'refs/heads/main')
        run: terraform output -json

      - name: Create summary
        if: always()
        run: |
          echo "## Terraform Deployment Summary" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "**Action:** ${{ github.event.inputs.action || 'plan' }}" >> $GITHUB_STEP_SUMMARY
          echo "**Branch:** ${{ github.ref_name }}" >> $GITHUB_STEP_SUMMARY
          echo "**Triggered by:** ${{ github.actor }}" >> $GITHUB_STEP_SUMMARY
          echo "" >> $GITHUB_STEP_SUMMARY
          echo "### Results" >> $GITHUB_STEP_SUMMARY
          echo "- Format: ${{ steps.fmt.outcome }}" >> $GITHUB_STEP_SUMMARY
          echo "- Init: ${{ steps.init.outcome }}" >> $GITHUB_STEP_SUMMARY
          echo "- Validate: ${{ steps.validate.outcome }}" >> $GITHUB_STEP_SUMMARY
          echo "- Plan: ${{ steps.plan.outcome || steps.plan-destroy.outcome }}" >> $GITHUB_STEP_SUMMARY
          if [[ "${{ github.event.inputs.action }}" == "apply" ]] || [[ "${{ github.event_name }}" == "push" && "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "- Apply: ✅ Completed" >> $GITHUB_STEP_SUMMARY
          elif [[ "${{ github.event.inputs.action }}" == "destroy" ]]; then
            echo "- Destroy: ✅ Completed" >> $GITHUB_STEP_SUMMARY
          fi
